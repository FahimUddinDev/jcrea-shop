"use client";

import LoginCard from "@/components/login/LoginCard";

export default function LoginPage() {
  return (
    <main className="flex min-h-[calc(100vh-66px)] items-center justify-center bg-slate-50 px-4">
      <LoginCard />
    </main>
  );
}
